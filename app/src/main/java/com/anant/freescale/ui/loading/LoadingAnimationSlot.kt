package com.anant.freescale.ui.loading

/** Where a measuring / wait animation may be shown. */
enum class LoadingAnimationSlot {
    /** Wide banner on the Home reading card during weigh-in / BIA. */
    READING,
}
